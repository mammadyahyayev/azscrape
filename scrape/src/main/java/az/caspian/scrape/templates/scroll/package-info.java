/**
 * The ScrollablePageTemplate is used to scrape data from pages which fetches data when you scroll
 * to the down (fetch on scroll). The process will continue until the scroll is reached to the end
 * of the page.
 */
package az.caspian.scrape.templates.scroll;
